
    Мое решение работает по такой логике:
    Сначала создаем таблицу в базе данных, если ее нет.
    Затем вставляем данные из json файла в эту таблицу.
    Находим офис,в котором работает указанный сотрудник.
    И находим всех сотрудников этого офиса.

Сложность алгоритма поиска данных сейчас k*n, где k-это глубина, а n-это количество записей.
Для оптимизации можно добавить индекс к полю parent_id(create index idx_ on department(parent_id)),что делает сложность k*log(n)
